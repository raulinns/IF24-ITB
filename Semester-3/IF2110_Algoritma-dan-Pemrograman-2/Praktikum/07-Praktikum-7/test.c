#include "ps-4/password.h"
#include <stdio.h>

/**
 * Fungsi untuk mendapatkan panjang angka
 *
 angka* Hint: gunakan log10
 *
 * kembalikan -1 jika angka == 0
 */

int main() {
    int angka;
    scanf("%d", &angka);
    findPassword(angka);
    return 0;
}
